name: Android Build

on:
  workflow_dispatch:
    inputs:
      android_version:
        description: 'Android 版本'
        required: true
        default: '14'
        type: choice
        options:
          - '12'
          - '13'
          - '14'
          - '15'
      build_type:
        description: '编译类型'
        required: true
        default: 'app'
        type: choice
        options:
          - 'app'
          - 'system'
      enable_cache:
        description: '启用编译缓存'
        required: false
        default: true
        type: boolean
      clean_build:
        description: '清理构建'
        required: false
        default: false
        type: boolean
      upload_artifacts:
        description: '上传构建产物'
        required: false
        default: true
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  CCACHE_DIR: /ccache
  USE_CCACHE: 1

jobs:
  android-build:
    runs-on: ubuntu-latest
    container:
      image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
      options: --user root
      volumes:
        - /tmp/ccache:/ccache

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup environment
        run: |
          # 设置环境变量
          echo "ANDROID_VERSION=${{ github.event.inputs.android_version }}" >> $GITHUB_ENV
          echo "BUILD_TYPE=${{ github.event.inputs.build_type }}" >> $GITHUB_ENV
          echo "ENABLE_CACHE=${{ github.event.inputs.enable_cache }}" >> $GITHUB_ENV
          echo "CLEAN_BUILD=${{ github.event.inputs.clean_build }}" >> $GITHUB_ENV
          
          # 显示环境信息
          echo "=== Build Environment ==="
          echo "Android Version: ${{ github.event.inputs.android_version }}"
          echo "Build Type: ${{ github.event.inputs.build_type }}"
          echo "Enable Cache: ${{ github.event.inputs.enable_cache }}"
          echo "Clean Build: ${{ github.event.inputs.clean_build }}"
          echo "========================="

      - name: Restore ccache cache
        if: ${{ github.event.inputs.enable_cache }}
        uses: actions/cache@v3
        with:
          path: /ccache
          key: ${{ runner.os }}-ccache-${{ hashFiles('**') }}
          restore-keys: |
            ${{ runner.os }}-ccache-

      - name: Restore Gradle cache
        if: ${{ github.event.inputs.enable_cache }}
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup ccache
        if: ${{ github.event.inputs.enable_cache }}
        run: |
          # 配置 ccache
          ccache -M 50G
          ccache -o compression=true
          ccache -o compression_level=6
          ccache -s

      - name: Verify build environment
        run: |
          # 验证环境
          ./scripts/verify-env.sh

      - name: Clean build directory
        if: ${{ github.event.inputs.clean_build }}
        run: |
          echo "Cleaning build directory..."
          rm -rf build/ out/ app/build/ .gradle/
          ./scripts/clean-cache.sh --all

      - name: Build Android Application
        if: ${{ github.event.inputs.build_type == 'app' }}
        run: |
          echo "Building Android Application for Android ${{ github.event.inputs.android_version }}..."
          
          # 创建示例 Android 项目（如果不存在）
          if [ ! -f "build.gradle" ] && [ ! -f "app/build.gradle" ]; then
            echo "Creating sample Android project..."
            
            # 创建项目结构
            mkdir -p app/src/main/java/com/example/app
            mkdir -p app/src/main/res/layout
            mkdir -p app/src/main/res/values
            
            # 创建 build.gradle 文件
            cat > build.gradle << 'EOF'
            // Top-level build file
            buildscript {
                repositories {
                    google()
                    mavenCentral()
                }
                dependencies {
                    classpath 'com.android.tools.build:gradle:8.2.0'
                }
            }
            
            allprojects {
                repositories {
                    google()
                    mavenCentral()
                }
            }
            
            task clean(type: Delete) {
                delete rootProject.buildDir
            }
            EOF
            
            # 创建 app/build.gradle 文件
            cat > app/build.gradle << 'EOF'
            plugins {
                id 'com.android.application'
            }
            
            android {
                namespace 'com.example.app'
                compileSdk 34
            
                defaultConfig {
                    applicationId "com.example.app"
                    minSdk 24
                    targetSdk 34
                    versionCode 1
                    versionName "1.0"
                }
            
                buildTypes {
                    release {
                        minifyEnabled false
                        proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
                    }
                }
                compileOptions {
                    sourceCompatibility JavaVersion.VERSION_17
                    targetCompatibility JavaVersion.VERSION_17
                }
            }
            
            dependencies {
                implementation 'androidx.appcompat:appcompat:1.6.1'
                implementation 'com.google.android.material:material:1.10.0'
                implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
            }
            EOF
            
            # 创建 MainActivity
            cat > app/src/main/java/com/example/app/MainActivity.java << 'EOF'
            package com.example.app;
            
            import androidx.appcompat.app.AppCompatActivity;
            import android.os.Bundle;
            
            public class MainActivity extends AppCompatActivity {
                @Override
                protected void onCreate(Bundle savedInstanceState) {
                    super.onCreate(savedInstanceState);
                    setContentView(R.layout.activity_main);
                }
            }
            EOF
            
            # 创建布局文件
            cat > app/src/main/res/layout/activity_main.xml << 'EOF'
            <?xml version="1.0" encoding="utf-8"?>
            <androidx.constraintlayout.widget.ConstraintLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context=".MainActivity">
            
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Hello Android!"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintLeft_toLeftOf="parent"
                    app:layout_constraintRight_toRightOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />
            
            </androidx.constraintlayout.widget.ConstraintLayout>
            EOF
            
            # 创建 strings.xml
            cat > app/src/main/res/values/strings.xml << 'EOF'
            <resources>
                <string name="app_name">Android Build Test</string>
            </resources>
            EOF
            
            # 创建 AndroidManifest.xml
            cat > app/src/main/AndroidManifest.xml << 'EOF'
            <?xml version="1.0" encoding="utf-8"?>
            <manifest xmlns:android="http://schemas.android.com/apk/res/android">
            
                <application
                    android:allowBackup="true"
                    android:icon="@mipmap/ic_launcher"
                    android:label="@string/app_name"
                    android:roundIcon="@mipmap/ic_launcher_round"
                    android:supportsRtl="true"
                    android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
                    <activity
                        android:name=".MainActivity"
                        android:exported="true">
                        <intent-filter>
                            <action android:name="android.intent.action.MAIN" />
                            <category android:name="android.intent.category.LAUNCHER" />
                        </intent-filter>
                    </activity>
                </application>
            
            </manifest>
            EOF
          fi
          
          # 构建 APK
          echo "Building APK..."
          chmod +x gradlew 2>/dev/null || ./gradlew wrapper 2>/dev/null || true
          
          if [ -f "gradlew" ]; then
            ./gradlew assembleDebug --no-daemon --console=plain
          else
            gradle assembleDebug --no-daemon --console=plain
          fi
          
          # 检查构建结果
          if [ -d "app/build/outputs/apk" ]; then
            echo "✅ APK built successfully!"
            find app/build/outputs/apk -name "*.apk" | head -5
          else
            echo "⚠️  No APK found in app/build/outputs/apk"
          fi

      - name: Build Android System (AOSP)
        if: ${{ github.event.inputs.build_type == 'system' }}
        run: |
          echo "Building Android System (AOSP) for Android ${{ github.event.inputs.android_version }}..."
          
          # 注意：完整的 AOSP 构建需要下载大量源码
          # 这里只进行环境验证和模拟构建
          echo "This would build Android ${{ github.event.inputs.android_version }} AOSP"
          echo "For full AOSP build, you need to sync the source code first:"
          echo "  repo init -u https://android.googlesource.com/platform/manifest -b android-${{ github.event.inputs.android_version }}.0_r1"
          echo "  repo sync -j8"
          echo "  source build/envsetup.sh"
          echo "  lunch aosp_arm-eng"
          echo "  make -j\$(nproc)"
          
          # 验证构建环境
          echo "Verifying AOSP build environment..."
          which make && make --version
          which ninja && ninja --version
          which java && java -version
          which python3 && python3 --version
          
          echo "✅ AOSP build environment is ready!"

      - name: Save ccache cache
        if: ${{ github.event.inputs.enable_cache }}
        uses: actions/cache@v3
        with:
          path: /ccache
          key: ${{ runner.os }}-ccache-${{ hashFiles('**') }}

      - name: Save Gradle cache
        if: ${{ github.event.inputs.enable_cache }}
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Upload build artifacts
        if: ${{ github.event.inputs.upload_artifacts && github.event.inputs.build_type == 'app' }}
        uses: actions/upload-artifact@v4
        with:
          name: android-build-artifacts
          path: |
            app/build/outputs/apk/
            app/build/outputs/bundle/
            build/reports/
          retention-days: 7

      - name: Show ccache statistics
        if: ${{ github.event.inputs.enable_cache }}
        run: |
          echo "=== ccache Statistics ==="
          ccache -s
          echo "========================"

      - name: Build summary
        run: |
          echo "=== Build Summary ==="
          echo "Build Type: ${{ github.event.inputs.build_type }}"
          echo "Android Version: ${{ github.event.inputs.android_version }}"
          echo "Cache Enabled: ${{ github.event.inputs.enable_cache }}"
          echo "Clean Build: ${{ github.event.inputs.clean_build }}"
          echo "Status: ${{ job.status }}"
          echo "====================="
          
          # 显示构建时间
          echo "Build completed at: $(date)"
          
          # 显示磁盘使用情况
          echo "Disk usage:"
          df -h /ccache ~/.gradle 2>/dev/null || true